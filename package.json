{
  "name": "bluslate",
  "version": "0.2.11",
  "private": true,
  "description": "Cross-platform tool to rename TV show files using TMDb metadata",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r run typecheck",
    "start": "pnpm --filter @bluslate/cli run start",
    "generate-icon": "node scripts/generate-icon.mjs",
    "package:gui": "pnpm --filter @bluslate/core run build && pnpm --filter @bluslate/gui run build && pnpm --filter @bluslate/gui run package",
    "dev:web": "pnpm --filter @bluslate/web run dev",
    "build:web": "pnpm --filter @bluslate/core run build && pnpm --filter @bluslate/web run build",
    "docker:build": "docker build -t bluslate .",
    "docker:run": "docker run -p 3000:3000 -v /path/to/media:/media bluslate"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "keywords": [
    "media",
    "rename",
    "tmdb",
    "tv",
    "shows",
    "cli"
  ],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.2",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@ffprobe-installer/darwin-arm64",
      "@ffprobe-installer/ffprobe",
      "electron",
      "esbuild",
      "sharp"
    ],
    "overrides": {
      "rollup": ">=4.59.0",
      "tar": ">=7.5.10",
      "electron-builder-squirrel-windows": ">=26.8.1",
      "minimatch@<3.1.3": ">=3.1.3",
      "minimatch@>=5.0.0 <5.1.7": ">=5.1.7",
      "minimatch@>=9.0.0 <9.0.6": ">=9.0.6",
      "minimatch@>=10.0.0 <10.2.1": ">=10.2.1"
    }
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "sharp": "^0.34.5",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
